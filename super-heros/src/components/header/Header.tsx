import React from 'react'
// On importe le css dans le fichier tsx
import './header.css'

// On créera une fonction Header, le nom de la fonction est en PascalCase (car c'est un composant)
const Header = () => {
  return (
    <h1>Mes super héros</h1>
  )
}

export default Header